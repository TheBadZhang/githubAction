name: 'MSVC 2019'

on:
  push:
    branches: master

jobs:
  build:
    name: 'build MSVC 2019'
    runs-on: windows-latest
    steps:
      - name: "complie src"
        run: |
          echo "set environment"
          echo $(get-location)
          cmake .
          make
          ./githubAction-release
